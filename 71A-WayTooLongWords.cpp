/*#include <iostream>

using namespace std;

int main()
{
	int n;
	cin >> n;

	string input;
	int length;
	
	while (n > 0)
	{
		cin >> input;

		length = input.length();

		if (length > 10)
		{
			length -= 2;
			cout << input.at(0) << length << input.back() << "\n";
		}
		else
			cout << input << "\n";

		n--;
	}

	return 0;
}*/