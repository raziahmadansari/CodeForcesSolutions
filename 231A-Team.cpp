/*#include <iostream>

using namespace std;

int main()
{
	int testCase;
	int petya, vasya, tonya;
	int count = 0;
	int totalQuestion = 0;

	cin >> testCase;

	for (int i = 0; i < testCase; i++)
	{
		cin >> petya >> vasya >> tonya;

		if (petya == 1)
			count++;
		if (vasya == 1)
			count++;
		if (tonya == 1)
			count++;

		if (count >= 2)
			totalQuestion++;

		count = 0;
	}

	cout << totalQuestion;

	return 0;
}*/